{
  "name": "clipper-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen --output-dir ./types",
    "build": "graph build",
    "deploy:polygon": "graph deploy --node https://api.thegraph.com/deploy/ edoapp/clipper-polygon --debug",
    "create-local": "graph create --node http://localhost:8020/ edoapp/clipper-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ edoapp/clipper-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 edoapp/clipper-polygon"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.0.0",
    "@typescript-eslint/parser": "^2.0.0",
    "eslint": "^6.2.2",
    "eslint-config-prettier": "^6.1.0",
    "prettier": "^1.18.2",
    "typescript": "^3.5.2"
  }
}
